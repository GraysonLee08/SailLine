// RacePickerScreen.tsx — DEPRECATED.
// Replaced by mobile/app/(app)/index.tsx (MapHomeScreen) which renders
// the race list via RaceListSheet over a Mapbox canvas.
// Safe to delete with `git rm mobile/src/screens/RacePickerScreen.tsx`.

export {};
