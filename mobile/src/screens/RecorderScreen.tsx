// RecorderScreen.tsx — DEPRECATED.
// Replaced by mobile/app/(app)/recording.tsx — map + GuidanceCard +
// Stop button, with live next-mark guidance during racing.
// Safe to delete with `git rm mobile/src/screens/RecorderScreen.tsx`.

export {};
