[pytest]
markers =
    slow: tests that hit real network or take noticeable time (e.g. live NOAA)

# Silences the pytest-asyncio deprecation warning seen in test runs.
asyncio_default_fixture_loop_scope = function
