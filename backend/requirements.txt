# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Config + validation
pydantic==2.9.2
pydantic-settings==2.5.2

# Weather data parsing — the high-risk dependency
cfgrib==0.9.14.1
xarray==2024.9.0
numpy==2.1.2
scipy==1.14.1

# Bathymetry — spatial polygon index for ENC hazards
shapely==2.0.6

# NetCDF reader. Required by the currents ingest worker (NOAA OFS forecast
# files are NetCDF4) and available to the bathymetry path as a fallback
# alongside xarray's default engine. ~10 MB on disk, pulls libhdf5.
# 1.7.2 ships prebuilt wheels for cp39 through cp313; 1.7.1 only goes
# up to cp312 and would force a source build on Python 3.13.
netCDF4==1.7.2

# Database (week 1 — Cloud SQL via Connector + asyncpg pool)
asyncpg==0.30.0
cloud-sql-python-connector[asyncpg]==1.11.0

# Schema migrations (Alembic CLI only — runtime app still uses asyncpg).
# psycopg is a sync driver; chosen here because migrations run from a shell,
# not the event loop, and sync is simpler than mixing asyncpg with Alembic.
alembic==1.13.3
psycopg[binary]==3.2.3

# Auth (week 1 — Firebase JWT verification)
firebase-admin==6.5.0

# Storage
google-cloud-storage==3.10.1
redis==5.1.1

# Testing
pytest==8.3.3
pytest-asyncio==1.3.0
httpx==0.28.1


# SSE for routing stream
sse-starlette==2.1.3

# WebSocket client for AISStream.io subscription (worker).
# Pinned explicitly rather than relying on uvicorn[standard]'s transitive
# pull because the AIS worker is a standalone process.
websockets==13.1




anthropic==0.39.0                 # Post-race AI summary (Session D1)
pypdf==5.1.0                      # MWPHRF cert parsing (Session D2)
python-multipart==0.0.20          # FastAPI UploadFile for /api/boats/{id}/cert
sendgrid==6.11.0                  # Transactional email for boat invites (Session D3)
email-validator==2.2.0            # Pydantic EmailStr backing (Session D3)
# stripe                            # Payments (week 7)