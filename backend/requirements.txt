# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Config + validation
pydantic==2.9.2
pydantic-settings==2.5.2

# Weather data parsing — the high-risk dependency
cfgrib==0.9.14.1
xarray==2024.9.0
numpy==2.1.2

# Database (week 1 — Cloud SQL via Connector + asyncpg pool)
asyncpg==0.30.0
cloud-sql-python-connector[asyncpg]==1.11.0

# Future weeks (commented out until needed):
# google-cloud-storage              # GCS (week 2)
# redis==5.1.1                      # Redis cache (week 2)
# firebase-admin                    # JWT verification (week 1-2)
# anthropic                         # Claude API (week 8)
# stripe                            # Payments (week 7)
# httpx                             # Async HTTP client for Datalastic (week 7)
# sse-starlette                     # SSE for routing stream (week 6)